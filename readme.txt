The first assumption made in models is that a tractor can only have one of each implements.

Also no tractor can be created without a corresponding Farmer.

Boolean Value is henced to store the presence or absence of implements.

Presence of implement is indicated with True or False